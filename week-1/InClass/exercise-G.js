const numberofstudents = 20
const numberofmentors = 3

const PercentageOfStudents = numberofstudents / 23 * 100
const PercentageOfMentors = numberofmentors / 23 * 100

const studentpercentage = Math.round(PercentageOfStudents)
const mentorpercentage = Math.round(PercentageOfMentors)

console.log ('The percentage of students is ' + studentpercentage + '%')
console.log ('The percentage of mentors is ' + mentorpercentage + '%')

